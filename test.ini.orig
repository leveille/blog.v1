#
# Wurdig - Pylons testing environment configuration
#
# The %(here)s variable will be replaced with the parent directory of this file
#
[DEFAULT]
debug = true
# Uncomment and replace with the address which should receive any error reports
email_to = leveillej@gmail.com
smtp_server = localhost
error_email_from = server.error@jasonleveille.com

[server:main]
use = egg:Paste#http
host = 127.0.0.1
port = 5000

[app:main]
use = egg:Wurdig
full_stack = true
static_files = true

cache_dir = %(here)s/data
beaker.session.key = wurdig
beaker.session.secret = "Wurdigs Holiday Acres"

# Add additional test specific configuration options as necessary.
